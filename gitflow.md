## GITFLOW
Con gitflow aprenderemos a manejar un repositorio colaborativo.


Nunca trabajamos sobre la rama main para agregar cambios, ya que estos pueden tener errores para eso creamos una rama llamada dev o development

Para crear la rama de desarrollo debemos usar 
    git checkout -b <nombre-nueva-rama> 